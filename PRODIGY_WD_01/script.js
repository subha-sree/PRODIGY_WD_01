// If you want to implement the hamburger menu on mobile devices
function toggleMenu() {
    const navIcons = document.querySelector('.nav-icons');
    navIcons.classList.toggle('active');
  }
  